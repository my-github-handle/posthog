apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "posthog.fullname" . }}-ingestion-sessionreplay
  labels:
    {{- include "posthog.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.ingestionSessionReplay.replicas }}
  selector: { matchLabels: { app: {{ include "posthog.fullname" . }}-ingestion-sessionreplay } }
  template:
    metadata:
      labels: { app: {{ include "posthog.fullname" . }}-ingestion-sessionreplay }
      annotations:
        checksum/app-secret: {{ include (print $.Template.BasePath "/app-secret.yaml") . | sha256sum }}
    spec:
      containers:
        - name: ingestion-sessionreplay
          image: {{ .Values.images.pluginServerNode | quote }}
          command: ["node", "nodejs/dist/index.js"]
          envFrom:
            - secretRef: { name: {{ include "posthog.fullname" . }}-app-secrets }
            - secretRef: { name: {{ .Values.gcs.hmacSecretName }} }
          env:
            # Kafka override (librdkafka env-reading workaround — see posthog-worker.yaml)
            - { name: KAFKA_HOSTS,       value: {{ .Values.kafka.hosts | quote }} }
            - { name: KAFKA_BROKER_LIST, value: {{ .Values.kafka.hosts | quote }} }
            - { name: KAFKA_PRODUCER_METADATA_BROKER_LIST, value: {{ .Values.kafka.hosts | quote }} }
            - { name: KAFKA_CONSUMER_METADATA_BROKER_LIST, value: {{ .Values.kafka.hosts | quote }} }
            # Per-cluster producer registry overrides (session-replay path)
            - { name: KAFKA_DEFAULT_PRODUCER_METADATA_BROKER_LIST,     value: {{ .Values.kafka.hosts | quote }} }
            - { name: KAFKA_WARPSTREAM_PRODUCER_METADATA_BROKER_LIST,  value: {{ .Values.kafka.hosts | quote }} }
            - { name: KAFKA_INGESTION_PRODUCER_METADATA_BROKER_LIST,   value: {{ .Values.kafka.hosts | quote }} }
            # Existing Confluent CP Kafka in c3-opsadmin does not bundle snappy native libs
            # (FIPS JDK blocks org.xerial.snappy load), so broker rejects snappy-compressed
            # produces with "unknown broker error". Force all librdkafka producers to none.
            - { name: KAFKA_PRODUCER_COMPRESSION_CODEC,              value: "none" }
            - { name: KAFKA_DEFAULT_PRODUCER_COMPRESSION_CODEC,      value: "none" }
            - { name: KAFKA_METRICS_PRODUCER_COMPRESSION_CODEC,      value: "none" }
            - { name: KAFKA_MSK_PRODUCER_COMPRESSION_CODEC,          value: "none" }
            - { name: KAFKA_WAREHOUSE_PRODUCER_COMPRESSION_CODEC,    value: "none" }
            - { name: KAFKA_CDP_PRODUCER_COMPRESSION_CODEC,          value: "none" }
            - { name: KAFKA_INGESTION_PRODUCER_COMPRESSION_CODEC,    value: "none" }
            - { name: KAFKA_WARPSTREAM_PRODUCER_COMPRESSION_CODEC,   value: "none" }
            - { name: KAFKA_WARPSTREAM_LOGS_PRODUCER_COMPRESSION_CODEC,      value: "none" }
            - { name: KAFKA_WARPSTREAM_INGESTION_PRODUCER_COMPRESSION_CODEC, value: "none" }
            # Postgres env block (shared pattern from posthog-web.yaml)
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.adminSecretName | quote }}, key: postgres-admin-username } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.adminSecretName | quote }}, key: postgres-admin-password } }
            - name: POSTGRES_HOST
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.adminSecretName | quote }}, key: postgres-db-endpoint } }
            - name: POSTGRES_PORT
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.adminSecretName | quote }}, key: postgres-db-port } }
            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/{{ .Values.postgres.database }}"
            - name: PERSONS_DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/{{ .Values.postgres.database }}"
            # Mode-specific env
            - { name: PLUGIN_SERVER_MODE, value: "recordings-blob-ingestion-v2" }
            - name: SESSION_RECORDING_V2_S3_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: {{ include "posthog.fullname" . }}-app-secrets
                  key: OBJECT_STORAGE_ENDPOINT
            - { name: SESSION_RECORDING_V2_S3_TIMEOUT_MS,       value: "120000" }
            - { name: SESSION_RECORDING_V2_S3_FORCE_PATH_STYLE, value: "true" }
            # Bucket + prefix — otherwise consumer defaults to upstream "posthog" bucket and 403s against GCS
            - { name: SESSION_RECORDING_V2_S3_BUCKET,            value: {{ .Values.gcs.bucket | quote }} }
            - { name: SESSION_RECORDING_V2_S3_PREFIX,            value: {{ .Values.gcs.sessionRecordingFolder | quote }} }
            - { name: OBJECT_STORAGE_BUCKET,                     value: {{ .Values.gcs.bucket | quote }} }
            - { name: OBJECT_STORAGE_SESSION_RECORDING_FOLDER,   value: {{ .Values.gcs.sessionRecordingFolder | quote }} }
            # ClickHouse Kafka engine subscribes to posthog_clickhouse_session_replay_events;
            # plugin-server's default topic lacks the prefix, so the metadata MV stays empty
            # and the Replay UI shows "no recordings" despite blobs in GCS.
            - { name: SESSION_RECORDING_V2_REPLAY_EVENTS_KAFKA_TOPIC, value: "posthog_clickhouse_session_replay_events" }
          resources:
            requests:
              cpu:    {{ (index .Values.defaultResources .Values.ingestionSessionReplay.resourcesTier).requests.cpu | quote }}
              memory: {{ (index .Values.defaultResources .Values.ingestionSessionReplay.resourcesTier).requests.memory | quote }}
            # No limits: blob ingestion aggregates rrweb snapshots in memory before flushing
            # to GCS; a memory cap causes OOM during bursty sessions.
