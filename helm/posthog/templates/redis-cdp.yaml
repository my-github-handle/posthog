apiVersion: v1
kind: Service
metadata:
  name: {{ include "posthog.fullname" . }}-redis-cdp
  labels:
    {{- include "posthog.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "posthog.fullname" . }}-redis-cdp
  ports:
    - { name: redis, port: 6379, targetPort: 6379 }
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "posthog.fullname" . }}-redis-cdp
  labels:
    {{- include "posthog.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "posthog.fullname" . }}-redis-cdp
  replicas: 1
  selector: { matchLabels: { app: {{ include "posthog.fullname" . }}-redis-cdp } }
  template:
    metadata:
      labels: { app: {{ include "posthog.fullname" . }}-redis-cdp }
    spec:
      containers:
        - name: redis
          image: {{ .Values.redisCdp.image | quote }}
          ports: [{ containerPort: 6379 }]
          args: ["redis-server"]
          resources:
            requests:
              cpu: {{ .Values.redisCdp.resources.requests.cpu | quote }}
              memory: {{ .Values.redisCdp.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.redisCdp.resources.limits.cpu | quote }}
              memory: {{ .Values.redisCdp.resources.limits.memory | quote }}
          volumeMounts: [{ name: data, mountPath: /data }]
  volumeClaimTemplates:
    - metadata: { name: data }
      spec:
        accessModes: [ReadWriteOnce]
        storageClassName: {{ .Values.redisCdp.storageClass | quote }}
        resources: { requests: { storage: {{ .Values.redisCdp.storageSize | quote }} } }
